/node_modules
/.next
/out
/build
.DS_Store
*.pem
npm-debug.log*
# Entorno: NO subir secretos. .env.example SÍ se versiona.
.env
.env*
!.env.example
.vercel
*.tsbuildinfo
next-env.d.ts

# Imágenes subidas desde el panel admin (runtime). Se versiona solo la carpeta.
/public/uploads/products/*
!/public/uploads/products/.gitkeep
